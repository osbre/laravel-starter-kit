## Prerequisites

If you haven't, install [Foreman](https://github.com/ddollar/foreman) - a Procfile manager:

```shell
gem install foreman
```

Then start all development services:

```shell
composer dev
```